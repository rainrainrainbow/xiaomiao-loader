# Xiaomiao ROM Loader partition table
# Flash: 4MB QIO 80MHz, partition table at 0x9000
#
# factory      - Loader firmware (persistent, never overwritten by OTA)
# launcher     - ROM slot 0 (single-app ROM, or retro-go launcher)
# retro-core   - ROM slot 1 (retro-go emulator core; label matches what
#                retro-go looks up via esp_partition_find_first by name)
# otadata      - Boot selection (loader sets to ota_0 before reboot;
#                ROM sets back to factory on startup for return-to-loader)

# Name,       Type, SubType, Offset,   Size,     Flags
nvs,          data, nvs,     0xA000,   0x5000,
phy_init,     data, phy,     0xF000,   0x1000,
factory,      app,  factory, 0x10000,  0xAE000,
otadata,      data, ota,     0xBE000,  0x2000,
launcher,     app,  ota_0,   0xC0000,  0x200000,
retro-core,   app,  ota_1,   0x2C0000, 0x140000,
